<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">
    <t t-name="mobile_form_builder.MFormSubmissionListView">
        <div t-att-class="className" t-ref="root">
            <Layout className="model.useSampleModel ? 'o_view_sample_data' : ''" display="display">
                <t t-set-slot="control-panel-create-button">
                    <button t-if="!editedRecord and activeActions.create and props.showButtons" type="button" class="btn btn-primary o_list_button_add" data-hotkey="c" t-on-click="onClickCreate" data-bounce-button="">
                        New
                    </button>
                    <t t-if="props.showButtons and !env.inDialog" t-call="web.ListView.EditableButtons"/>
                </t>

                <t t-set-slot="layout-buttons">
                    <t t-if="props.showButtons and env.inDialog" t-call="web.ListView.EditableButtons"/>
                    <t t-if="props.showButtons" t-call="{{ props.buttonTemplate }}"/>
                </t>
                <t t-set-slot="control-panel-always-buttons">
                    <t t-foreach="archInfo.headerButtons" t-as="button" t-key="button.id" t-if="!evalViewModifier(button.invisible)">
                        <MultiRecordViewButton
                            t-if="button.display === 'always'"
                            list="model.root"
                            className="button.className"
                            clickParams="button.clickParams"
                            defaultRank="'btn-secondary'"
                            domain="props.domain"
                            icon="button.icon"
                            string="button.string"
                            title="button.title"
                            attrs="button.attrs"
                        />
                    </t>
                </t>

                <t t-set-slot="layout-actions">
                    <div class="d-flex flex-column w-100 gap-2">
                        <div>
                            <SearchBar toggler="searchBarToggler" autofocus="firstLoad" />
                        </div>
                        <div class="d-flex justify-content-end">
                            <t t-if="!hasSelectedRecords" t-call="mobile_form_builder.SubmissionListButtons" />
                        </div>
                    </div>
                </t>
                <t t-set-slot="control-panel-navigation-additional">
                    <t t-if="!hasSelectedRecords" t-component="searchBarToggler.component" t-props="searchBarToggler.props"/>
                </t>

                <t t-set-slot="control-panel-additional-actions">
                    <CogMenu t-if="!hasSelectedRecords"/>
                    <CogMenu t-elif="env.isSmall and (props.info.actionMenus or archInfo.headerButtons.length)" t-props="this.actionMenuProps" hasSelectedRecords="hasSelectedRecords">
                        <t t-foreach="archInfo.headerButtons" t-as="button" t-key="button.id" t-if="!evalViewModifier(button.invisible) and ((button.clickParams and button.clickParams.name) != 'action_unconfirm' or !(model.root.selection and model.root.selection.length &gt; 1))">
                            <DropdownItem class="'o-dropdown-item-unstyled-button'">
                                <MultiRecordViewButton
                                    t-if="button.display !== 'always'"
                                    list="model.root"
                                    className="button.className"
                                    clickParams="button.clickParams"
                                    defaultRank="'btn-secondary'"
                                    domain="props.domain"
                                    icon="button.icon"
                                    string="button.string"
                                    title="button.title"
                                    attrs="button.attrs"
                                />
                            </DropdownItem>
                        </t>
                    </CogMenu>
                </t>
                <t t-set-slot="control-panel-selection-actions">
                    <div t-if="hasSelectedRecords" class="o_selection_container m-1 m-md-0 d-flex gap-1">
                        <SelectionBox root="this.model.root"/>
                        <t t-if="!env.isSmall">
                            <t t-foreach="archInfo.headerButtons" t-as="button" t-key="button.id" t-if="!evalViewModifier(button.invisible) and ((button.clickParams and button.clickParams.name) != 'action_unconfirm' or !(model.root.selection and model.root.selection.length &gt; 1))">
                                <MultiRecordViewButton
                                    t-if="button.display !== 'always'"
                                    list="model.root"
                                    className="button.className"
                                    clickParams="button.clickParams"
                                    defaultRank="'btn-secondary'"
                                    domain="props.domain"
                                    icon="button.icon"
                                    string="button.string"
                                    title="button.title"
                                    attrs="button.attrs"
                                />
                            </t>
                            <t t-if="props.info.actionMenus">
                                <ActionMenus t-props="this.actionMenuProps"/>
                            </t>
                        </t>
                    </div>
                </t>
                <t t-component="props.Renderer" t-if="model.isReady"
                    list="model.root"
                    activeActions="activeActions"
                    archInfo="archInfo"
                    allowSelectors="props.allowSelectors"
                    editable="editable"
                    onOpenFormView="onOpenFormView"
                    hasOpenFormViewButton="hasOpenFormViewButton"
                    openRecord.bind="openRecord"
                    noContentHelp="props.info.noContentHelp"
                    onAdd.bind="createRecord"
                    optionalActiveFields="optionalActiveFields"
                    readonly="props.readonly"
                />
            </Layout>
        </div>
    </t>

    <t t-name="mobile_form_builder.SubmissionListButtons">
        <div class="d-flex flex-column flex-md-row align-items-stretch align-items-md-center gap-2 w-100">
            <input
                type="text"
                class="form-control"
                style="width: 100%; max-width: 420px; height: 34px;"
                placeholder="Confirm by code..."
                t-att-value="(this.state &amp;&amp; this.state.code) || ''"
                t-ref="mformCodeInput"
                t-on-input="onMformCodeInput"
                t-on-keydown="onMformCodeKeydown"
            />
            <div class="d-flex align-items-center gap-2">
                <button
                    type="button"
                    class="btn btn-outline-secondary btn-sm"
                    style="height: 34px; padding: 0 10px; font-size: 12px; line-height: 1; white-space: nowrap;"
                    t-on-click="onMformScanClick"
                >
                    Scan
                </button>
                <input
                    type="file"
                    accept="image/*"
                    capture="environment"
                    t-ref="mformScanInput"
                    t-on-change="onMformScanFileChange"
                    style="display:none;"
                />
                <button
                    type="button"
                    class="btn btn-primary btn-sm"
                    style="height: 34px; padding: 0 10px; font-size: 12px; line-height: 1; white-space: nowrap;"
                    t-on-click="onMformConfirmByCode"
                >
                    Confirm
                </button>
            </div>
        </div>
        <div t-if="this.state &amp;&amp; this.state.scannerOpen" class="position-fixed top-0 start-0 w-100 h-100 d-flex align-items-center justify-content-center" style="z-index: 2000; background: rgba(0,0,0,0.65);">
            <div class="bg-white rounded p-2" style="width: min(92vw, 420px);">
                <div class="d-flex justify-content-between align-items-center mb-2">
                    <strong>Scan Code</strong>
                    <button type="button" class="btn btn-sm btn-outline-secondary" t-on-click="onMformCloseScanner">Close</button>
                </div>
                <div style="position: relative; overflow: hidden; border-radius: 6px; background: #111; height: min(58vh, 420px);">
                    <video t-ref="mformScanVideo" autoplay="autoplay" playsinline="playsinline" muted="muted" style="width: 100%; height: 100%; object-fit: cover; transform: scale(1.5); transform-origin: center center;"/>
                    <div style="position:absolute; left:50%; top:8%; bottom:8%; width:2px; background:#e02020; transform:translateX(-50%); pointer-events:none; opacity:0.9;"/>
                    <div style="position:absolute; top:50%; left:8%; right:8%; height:2px; background:#e02020; transform:translateY(-50%); pointer-events:none; opacity:0.9;"/>
                </div>
                <canvas t-ref="mformScanCanvas" style="display:none;"/>
                <div class="mt-2 text-muted" style="font-size:12px;">
                    Auto scanning... align the code with the red cross lines.
                </div>
            </div>
        </div>
    </t>
</templates>
